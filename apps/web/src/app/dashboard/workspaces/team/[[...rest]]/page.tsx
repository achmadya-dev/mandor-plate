'use client';

import PageContainer from '@/components/layout/page-container';

export default function TeamPage() {
  return (
    <PageContainer pageTitle='Team' pageDescription='Team management placeholder'>
      <p className='text-muted-foreground text-sm'>Clerk organization UI removed. Out of scope.</p>
    </PageContainer>
  );
}
